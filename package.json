{
  "devDependencies": {
    "eslint": "^10.2.0",
    "jest": "^30.3.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0 || true",
    "test": "jest --passWithNoTests",
    "build": "tsc"
  }
}